# v0.2：发布与复验

[English](v0.2-release.en.md) | 简体中文

本次发布将四阶段实验整理为可检查的代码、公开报告和完整证据归档。软件版本为 0.2.0；提交历史分别记录 v0.1 DeepNote 补充、v0.2 E1–E4 实验与 PilotDeck 收尾研究。

## 研究结果与范围

E3 中，utility_aware 相对冻结 importance 的三主预算平均 EM 差异为 +0.2014，描述性 95% 区间 [0.0972, 0.3056]。E4 显示：冗余证据的 LOO 目标可能无法区分好坏集合；精确预测分数和选择没有改善本实验平均分。

这些是固定 MiniCPM 系统、四类等比例合成压力场景下的结果。每条轨迹只有四条 memory 和两个未来任务，不证明自然用户泛化、在线长期收益或图结构方法的收益。参见 [E3](../releases/v0.2.0/reports/e3-formal-model/report.md) 与 [E4](../releases/v0.2.0/reports/e4-diagnostic-model/report.md)。

## 发布文件边界

| 内容 | 位置与处理 |
|---|---|
| 活跃代码、协议、配置、数据集、测试 | 正常版本控制 |
| 公开 manifest、报告、审计、汇总与图表 | releases/v0.2.0/reports；明确文件清单见 published-artifacts.json |
| 完整原始输出、前序依赖及历史源码快照 | frozen-experiments.tar.gz，仅保存一份；frozen-index.json 逐文件校验 |
| 本地 results、重复源码快照 | 通过恢复命令生成，Git 默认忽略，不删除现有本地结果 |
| 模型权重、索引、缓存 | 不发布 |

归档约 1.7 MB，保留 412 个原始文件。pilot 与开发结果不作为公开主结果展示，但在完整证据归档中保留；不能将其一概删除，因为前序依赖与 provenance 有用。v0.1 冻结结果与忽略规则未改动。

## 从干净检出复验

按[使用指南](usage.md)安装依赖后，从仓库根目录执行以下命令，无需模型或 GPU。Study 核验需要指南中说明的历史提交：

```bash
python scripts/release_artifacts.py verify
python scripts/release_artifacts.py restore-results
python scripts/audit_v02.py
python experiments/utility-retention/audits/verify_e4.py experiments/utility-retention/results/e4-diagnostic-model
PYTHONPATH=src python studies/verify.py
python -m pytest -q
python -m ruff check src tests scripts integrations experiments/utility-retention studies --exclude '**/results/**'
```

恢复会先验证所有文件，拒绝覆盖不同内容和越界路径。审计重算标签、预测、选择、评分、配对统计与集合诊断；不重新生成答案，不将历史 GPU 重放记录冒充本轮新推理。

## 原始源码与兼容审计

原始 manifest、源码快照、回答及派生文件保持逐字节不变。source-compatibility.json 将已审阅的源码移动、公共评分、后端别名和诊断 adapter 关联到当前完整实现的摘要；任何未登记变更都不通过该映射。该映射不声称当前源码与历史源码字节相同。

独立恢复可重新使用历史源码，不依赖兼容映射：

```bash
python scripts/release_artifacts.py restore --output /tmp/utility-frozen-e1 --source-run e1-extension-model
PYTHONPATH=/tmp/utility-frozen-e1/src .venv/bin/python /tmp/utility-frozen-e1/experiments/utility-retention/evaluate.py /tmp/utility-frozen-e1/experiments/utility-retention/results/e1-extension-model
```

`--source-run` 选择该运行 manifest 所记录的源码变体，不改动当前目录。历史两个 pilot 的原 runner 没有完整保存，因此只能验证它们的现存回答与标签，不能声称可恢复其精确源码；冻结 E1 与后续主实验无此已知缺口。

E1 扩展强审计现在使用公共模型配置，CPU 默认检查与 tokenizer 重算明确分开：

```bash
python experiments/utility-retention/audits/verify_run.py experiments/utility-retention/results/e1-extension-model
python experiments/utility-retention/audits/verify_run.py experiments/utility-retention/results/e1-extension-model --verify-tokens --model-config configs/local.yaml
```

第二条命令需要原模型身份匹配的本地模型。本次发布收敛没有重新运行该 GPU/模型路径。

## 实现兼容性

- E1 数据构造、验证、报告和诊断共用 `utility_retention` package，顶层 CLI 保持兼容。各阶段复用公共 I/O、模型后端、selector、预测与响应校验，同时保留独立生命周期。
- 新删除计划使用 `intervention`，只读 adapter 保留冻结 `candidate` 记录。后端兼容连字符与下划线别名；共享 `longmem.scoring` 通过 600 个归档回答与未修改的 v0.1 scorer 校验一致性。
- 软件与 manifest envelope 分别演进。跨版本 baseline 变体、预算单位和检索实现遵循显式[发布契约](../releases/v0.2.0/release-contracts.json)。

## 验证状态

**v0.2.0 最终发布验收通过 104 项测试。** 初次干净检出验证通过 103 项，发布前新增一项数值复验回归测试。冻结的 [validation.json](../releases/v0.2.0/validation.json) 记录初次检查，不代表最终测试数量。

已发布的 [v0.2.0](https://github.com/TOMDFTBA/agent-memory-utility/releases/tag/v0.2.0) 指向 `d96521d5eef3ddfae9a4949c6c8ba395c6821637`。对应[云端 CI](https://github.com/TOMDFTBA/agent-memory-utility/actions/runs/34787454334) 在 Python 3.11 和 3.12 上通过，覆盖证据恢复、E1–E4 审计、独立 E4 验算、study 源码核验与 CPU 测试；Ruff 也已通过。

数值复验使用明确的浮点容差，同时保持产物身份与离散决策检查，见[数值复验说明](numerical-replay.md)。DeepNote/PilotDeck 检查仍为无模型证据；这些验证不增加第二台主机的 GPU 运行结论。

文风基线和早期检查见[发布过程记录](v0.2-publication-history.md)。后续模型、prompt 或选择行为改变必须使用新输出与新实验身份。
